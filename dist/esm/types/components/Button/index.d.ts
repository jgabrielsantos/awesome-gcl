import React from 'react';
import { ButtonComponentPropTypes } from './types';
export declare const Button: ({ children, type, size, customTheme, handleClick, disabled, className }: Readonly<ButtonComponentPropTypes>) => React.JSX.Element;
