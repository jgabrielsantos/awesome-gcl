{
  "restartable": "rs",
  "watch": ["src"],
  "env": {
    "NODE_ENV": "development"
  },
  "ext": "js, ts, tsx",
  "exec": "rm -rf dist && npx tailwindcss -i ./src/styles/styles.css -o ./dist/types/styles/styles.css && rollup -c"
}