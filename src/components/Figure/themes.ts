const figure = () => new Map([
  ['width', 'w-full'],
  ['height', 'h-full'],
])

const image = () => new Map([
  ['width', 'w-full'],
  ['height', 'h-full'],
])

const caption = () => new Map([
  ['font-size', 'text-base']
])

export default {
  figure,
  image,
  caption
}