const figureRule = () => new Map([
  ['width', 'w-full'],
  ['height', 'h-full'],
])

const imageRule = () => new Map([
  ['width', 'w-full'],
  ['height', 'h-full'],
])

const captionRule = () => new Map([
  ['font-size', 'text-base']
])

export default {
  figureRule,
  imageRule,
  captionRule
}